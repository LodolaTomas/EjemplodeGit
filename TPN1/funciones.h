/** \brief Calculo la suma de dos numeros
 *
 * \param float Recibo el primer numero
 * \param float Recibo el segundo numero
 * \return float Devulevo la suma entre ambos numeros osea el resultado
 *
 */
float funSuma(float,float);
/** \brief Calculo la resta de los numeros igresados
 *
 * \param float Primer numero Ingresado
 * \param float Segundo numero Ingresado
 * \return float Devuelvo el resultado de la resta
 *
 */
float funRestar(float,float);
/** \brief Calculo la multiplicacion de dos numeros ingresados
 *
 * \param float Numero uno ingresado
 * \param float Numero dos ingresado
 * \return float Devulevo la multiplicacion hecha
 *
 */
float funMultiplicar(float,float);
/** \brief Divide los numeros ingresados
 *
 * \param float Recibo el primer Numero
 * \param float Recibo el segundo Numero
 * \return float Devuelvo la division de ambos numeros
 *
 */
float funDividir(float,float);
/** \brief Saco el Factorial del numero Ingresado
 *
 * \param float Numero que quiero sacar factorial
 * \return float Devuelvo el resultado y en caso de no poder sacar el factorial devuelvo 0
 *
 */
float funFactorial(int);
/** \brief Pido un numero
 *
 * \return int Devuelvo el numero ingresado
 *
 */
float funOPC1();
/** \brief Pido otro numero
 *
 * \return float Devuelvo el numero ingresado
 *
 */
float funOPC2();
/** \brief Hago las operaciones
 *
 * \param float Ingreso el numero para hacer los calculos
 * \param float ingreso el otro numero para hacer los calculos
 * \return void ---
 *
 */
void funOPC3(float,float);
/** \brief Muestro las operaciones
 *
 * \param float Ingreso el numero para poder mostrar los calculos
 * \param float Ingreso el otro numero para poder mostrar los calculos
 * \return void ---
 *
 */
void funOPC4(float,float);
/** \brief Verifico que el numero sea entero para poder factorizarlo
 *
 * \param  Ingreso un numero para ver si es entero
 * \return Devuelvo 1 si el numero es entero y 0 sino lo es
 *
 */
int esEntero (float);

